apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-svc" .Release.Name }}
  namespace: {{ .Values.namespace }}
  labels:
    owner: {{ .Values.owner }}
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - port: 80
      targetPort: 8000
